<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <!-- Révocation des JWT : version de jeton par utilisateur. Le JWT embarque la valeur (claim "tv") ;
         incrémenter cette colonne invalide immédiatement tous les jetons émis avant (déconnexion globale). -->
    <changeSet id="102-user-token-version" author="claude">
        <addColumn tableName="users">
            <column name="token_version" type="int" defaultValueNumeric="0">
                <constraints nullable="false"/>
            </column>
        </addColumn>
    </changeSet>

</databaseChangeLog>
