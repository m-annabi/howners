package com.howners.gestion.domain.signature;

public enum SignatureType {
    SIMPLE,      // Signature simple (canvas)
    ELECTRONIC,  // Signature électronique
    QUALIFIED    // Signature qualifiée (eIDAS)
}
