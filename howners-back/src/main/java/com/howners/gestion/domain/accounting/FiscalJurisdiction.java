package com.howners.gestion.domain.accounting;

/**
 * Juridiction fiscale d'une activité. La V1 ne gère que la France ; l'énumération
 * existe dès maintenant pour que le moteur fiscal soit extensible (Suisse en V2).
 */
public enum FiscalJurisdiction {
    FR
    // SUISSE (V2)
}
